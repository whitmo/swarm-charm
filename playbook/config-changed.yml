---
- debug: msg="config changed"

- name: ensure swarm container
  docker:
    name: swarm
    image: swarm
    state: present

- set_fact:
    join_command: "{{ansible_local.run.command.join|default('')}}"
    manage_command: "{{ansible_local.run.command.manage|default('')}}"
    reload: "{{ansible_local.run.state.reload|default('')}}"

# if reloaded is reset, notify
- name: reloaded
  ini_file: "dest=/etc/ansible/facts.d/run.fact
             section=state
             option=reload
             value=''
             backup=yes"
  notify:
    - join cluster
    - manage cluster

