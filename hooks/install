#!/bin/bash

set -e
easy_install pip
apt-get install -y socat
pip install -r hooks/requirements.txt
mkdir -p /etc/ansible/facts.d

cat <<EOF > /etc/ansible/facts.d/swarm.fact
[command]
join=
manage=
[state]
reload=
EOF
